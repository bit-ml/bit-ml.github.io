# Conda environment for website maintenance scripts (poster PDF conversion, academic teasers).
# Create:  conda env create -f environment.yml
# Activate: conda activate bit-ml-website
name: bit-ml-website
channels:
  - conda-forge
dependencies:
  - python=3.12
  - pymupdf>=1.24
